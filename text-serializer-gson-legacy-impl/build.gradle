dependencies {
  compileOnly(libs.google.gsonNew) // needed for module definition
  api(projects.adventureApi)
  api(projects.adventureTextSerializerGson)
  api(projects.adventureNbt)
}

applyJarMetadata(this, "net.kyori.adventure.text.serializer.gson.legacyimpl")
