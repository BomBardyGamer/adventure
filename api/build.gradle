configurations.testCompileOnly {
  extendsFrom(configurations.compileOnlyApi)
}

dependencies {
  api(projects.adventureKey)
  api(libs.examination.api)
  api(libs.examination.string)
  compileOnlyApi(libs.checker)
  compileOnlyApi(libs.jetbrainsAnnotations)
  testImplementation(libs.google.guava)
}

sourceSets {
  main {
    multirelease {
      alternateVersions(10) // 9 added in root project
    }
  }
}

applyJarMetadata(this, "net.kyori.adventure")
