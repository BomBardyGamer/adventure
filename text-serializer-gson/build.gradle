dependencies {
  api(projects.adventureApi)
  compileOnly(libs.google.gsonNew)
  api(libs.google.gson)
  testImplementation(projects.adventureNbt)
}

applyJarMetadata(this, "net.kyori.adventure.text.serializer.gson")
